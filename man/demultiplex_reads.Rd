% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demultiplex.R
\name{demultiplex_reads}
\alias{demultiplex_reads}
\title{Demultiplex Reads by MID and Primer Combinations}
\usage{
demultiplex_reads(
  reads_df,
  mid_tags,
  primers,
  mid_length = 6L,
  primer_length = 18L
)
}
\arguments{
\item{reads_df}{Data frame from \code{\link{filter_fastq_reads}} (must have
columns \code{mid_tag} and \code{primer}).}

\item{mid_tags}{Character vector of MID tag sequences (order defines bin
numbering).}

\item{primers}{Character vector of primer sequences (order defines sub-bin
lettering: a, b, c, ...).}

\item{mid_length}{Integer. Length of MID tag (default 6).}

\item{primer_length}{Integer. Length of primer (default 18).}
}
\value{
The input data frame with an additional \code{bin_id} column
  (e.g., "1a", "1b", "2a", "2b", ...) and \code{bin_numeric} (integer
  bin number).
}
\description{
Assigns filtered reads to bins based on their MID × primer
combination. Replaces \code{bin_all_fwd.pl} and \code{bin_all_rev.pl}.
}
