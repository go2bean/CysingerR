% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastq_filter.R
\name{filter_fastq_reads}
\alias{filter_fastq_reads}
\title{Filter FASTQ Reads by MID Tags and Primers}
\usage{
filter_fastq_reads(
  fastq_file,
  mid_tags,
  primers,
  sequencer_prefix = "@M",
  max_padding = 4L,
  direction = c("forward", "reverse")
)
}
\arguments{
\item{fastq_file}{Path to a FASTQ file.}

\item{mid_tags}{Character vector of MID tag sequences (6-mer barcodes).}

\item{primers}{Character vector of primer sequences.}

\item{sequencer_prefix}{Character string for the sequencer header prefix
(default \code{"@M"}).}

\item{max_padding}{Integer. Maximum allowed random padding bases before the
MID tag (default 4).}

\item{direction}{Character. Either \code{"forward"} or \code{"reverse"}.}
}
\value{
A data frame with columns: \code{read_id}, \code{sequence},
  \code{quality}, \code{mid_tag}, \code{primer}, \code{direction},
  \code{classified} (logical).
}
\description{
Parses paired-end FASTQ files, identifies reads matching known
MID (Multiplex Identifier) tags and primer sequences, strips padding/spacer
nucleotides, and returns classified reads. Replaces the Perl scripts
\code{initial_filter_fwd.pl} and \code{initial_filter_rev.pl}.
}
\examples{
# See vignette for full examples
}
